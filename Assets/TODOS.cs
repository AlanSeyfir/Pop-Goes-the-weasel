using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TODOS : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //TODO: Fix the change of scene make it more natural not abruptly
        //TODO: Add a fitting song for the MainMenu 
        //TODO: Maybe to add in the button No a highscore table
        //TODO: Make the coins to spawn between 1 and 4 y axis
        //TODO: Review why Katon suddenly get a BennyCoin although it doesn't hit the hitbox
        //TODO: Create the UI, scenes , options, GameOver, Start
        //TODO: In gameOver make a highscore table
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
